// =========================
// economyConfig.js
// ค่าตั้งค่า "เศรษฐกิจ" ทั้งหมดของแอดออนที่เป็นตัวเลข/ธุรกิจ (ไม่ใช่ข้อมูล
// ไอเทม) รวมไว้จุดเดียว — ย้ายมาจากค่าคงที่ที่กระจายอยู่คนละไฟล์เดิม:
//   - BUY_PRICE_MULTIPLIER: เดิมอยู่ใน core/ItemDatabase.js
//   - TRANSFER.*: เดิมอยู่ใน systems/economy.js (TRANSFER_COOLDOWN /
//     TRANSFER_DISTANCE / MAX_LOGS / ค่า tax เริ่มต้น 0.10)
//   - TELEPORT.*: เดิมอยู่ใน systems/tpBankSystem.js (COST_PER_BLOCK /
//     CROSS_DIMENSION_COST / TP_REQUEST_TIMEOUT)
//   - AUTO_COLLECT.*: เดิม hardcode อยู่ใน systems/autoCollect.js
//     (startMoneyDrainInterval — ค่าใช้จ่ายต่อวินาทีของแต่ละโหมด)
//
// แก้ค่าตรงนี้จุดเดียว ไม่ต้องไล่หาเลขใน systems/*.js — ค่าที่แก้ตรงนี้
// จะมีผลทันทีทุกระบบที่ import ไปใช้ (ไม่มีเลขซ้ำกันคนละไฟล์อีกต่อไป)
// =========================

export const ECONOMY_CONFIG = {
  /** ตัวคูณคำนวณราคาซื้อจากราคาขายของไอเทมในฐานข้อมูล (buyPrice = ceil(sellPrice * BUY_PRICE_MULTIPLIER)) */
  BUY_PRICE_MULTIPLIER: 1.4,

  /** โอนเงินระหว่างผู้เล่น (systems/economy.js) */
  TRANSFER: {
    COOLDOWN_SECONDS: 5,
    MAX_DISTANCE: 150,
    DEFAULT_TAX_RATE: 0.10,
    MAX_LOGS: 50
  },

  /** Teleport แบบขอ-ยอมรับ (systems/tpBankSystem.js) */
  TELEPORT: {
    COST_PER_BLOCK: 10,
    CROSS_DIMENSION_COST: 10000,
    REQUEST_TIMEOUT_SECONDS: 30,

    // เวลานับถอยหลังหลังผู้รับกดยอมรับ ก่อนผู้ส่งจะถูกวาปจริง (วินาที) —
    // ระหว่างนี้ผู้ส่งต้องยืนนิ่ง ๆ ห้ามขยับ/ห้ามโดนโจมตี ไม่งั้นถูกยกเลิก
    // (คืนเงินให้เต็มจำนวน)
    CHANNEL_SECONDS: 12,

    // ระยะที่ขยับได้โดยไม่ถือว่า "ขยับ" ระหว่างนับถอยหลัง (กันการสั่นของ
    // ตำแหน่งเล็กน้อย เช่น การหันกล้อง/knockback เบา ๆ ที่ไม่ได้ตั้งใจขยับจริง)
    CHANNEL_MOVE_THRESHOLD_BLOCKS: 0.6,

    // ผู้เล่นที่เพิ่งโดนโจมตี หรือเพิ่งโจมตีใครไป ("กำลังต่อสู้อยู่") จะถูก
    // ปฏิเสธคำขอ TP อัตโนมัติเป็นเวลาเท่านี้ (วินาที) นับจากการต่อสู้ครั้ง
    // ล่าสุด — ป้องกันคำขอ TP กวนใจ/ขัดจังหวะตอนกำลังสู้อยู่ (ปิดได้เองต่อ
    // ผู้เล่นผ่านปุ่มในเมนู TP — ดู tpCombatBlockDisabled dynamic property)
    COMBAT_BLOCK_DURATION_SECONDS: 10
  },

  /** บ้าน/จุดวาปส่วนตัว (systems/homeSystem.js) */
  HOME: {
    // จำนวนบ้านสูงสุดต่อผู้เล่นหนึ่งคน — ตั้งบ้านใหม่เกินจำนวนนี้ไม่ได้
    // จนกว่าจะลบบ้านเก่าออกก่อน (ดู /prakan:delhome)
    MAX_HOMES: 5,

    // ค่าใช้จ่ายตอนตั้ง "บ้านใหม่" (ไม่ใช่การย้ายบ้านเดิมที่มีอยู่แล้ว —
    // อัปเดตตำแหน่งบ้านชื่อเดิมไม่คิดเงินซ้ำ เพราะไม่ได้เพิ่มจำนวนบ้าน)
    // ค่าเริ่มต้น 0 = ตั้งบ้านฟรีเหมือนคำสั่งวานิลลาทั่วไป แก้เป็นตัวเลข
    // มากกว่า 0 เพื่อเก็บค่าธรรมเนียมได้
    SET_HOME_COST: 1000,

    // ค่าเดินทางตอน "วาปไปบ้าน" (teleportHome) — แยกจาก TELEPORT.* ด้านบน
    // เพื่อให้ปรับราคาเฉพาะระบบบ้านได้อิสระ ไม่กระทบราคา /prakan:tpa
    // (ค่าเริ่มต้นตั้งเท่ากับ TELEPORT ไว้ก่อน แก้ตัวเลขด้านล่างนี้ได้เลย)
    //   - COST_PER_BLOCK: ราคาต่อบล็อกตอนวาปในมิติเดียวกัน (ตั้ง 0 = ฟรี)
    //   - CROSS_DIMENSION_COST: ราคาคงที่ตอนวาปข้ามมิติ ไม่คิดตามระยะทาง
    //     (ตั้ง 0 = ฟรี)
    COST_PER_BLOCK: 5,
    CROSS_DIMENSION_COST: 10000,

    // เวลานับถอยหลังก่อนวาปจริงหลังสั่ง /prakan:home (วินาที) — เหมือน
    // ระบบ channel ของ TELEPORT ด้านบน: ต้องยืนนิ่ง ห้ามเปลี่ยนมิติ ห้าม
    // โดนโจมตี ระหว่างนับถอยหลัง ไม่งั้นถูกยกเลิก (คืนเงินค่าเดินทางเต็ม
    // จำนวนถ้ามีการหักไปแล้ว)
    CHANNEL_SECONDS: 8,

    // ระยะที่ขยับได้โดยไม่ถือว่า "ขยับ" ระหว่างนับถอยหลัง (ค่าเดียวกับ
    // TELEPORT.CHANNEL_MOVE_THRESHOLD_BLOCKS ด้านบน — กันการสั่นของตำแหน่ง
    // เล็กน้อยที่ไม่ได้ตั้งใจขยับจริง)
    CHANNEL_MOVE_THRESHOLD_BLOCKS: 0.6,

    // คูลดาวน์ระหว่างการใช้คำสั่งระบบบ้านแต่ละครั้ง (วินาที) — กันสแปม
    // /prakan:sethome, /prakan:home, /prakan:delhome รัว ๆ ติดกัน คูลดาวน์
    // ใช้ร่วมกันทั้งสามคำสั่ง (ใช้คำสั่งไหนก็ติดคูลดาวน์คำสั่งอื่นด้วย ไม่ใช่
    // แยกคูลดาวน์ต่อคำสั่ง) — ตั้ง 0 เพื่อปิดการเช็คคูลดาวน์นี้
    COMMAND_COOLDOWN_SECONDS: 3
  },

  /** เก็บของ/ดูด XP/ดูดไอเทมอัตโนมัติ (systems/autoCollect.js) — ค่าใช้จ่ายต่อวินาทีขณะเปิดใช้งาน */
  AUTO_COLLECT: {
    ITEM_COST_PER_SECOND: 1,
    XP_COST_PER_SECOND: 1,
    VACUUM_COST_PER_SECOND: 10,
    // ส่งไอเทมที่เก็บได้ (จาก item/vacuum) ไปยังพิกัดที่ตั้งไว้แทนกระเป๋าผู้เล่น
    XYZ_SEND_COST_PER_SECOND: 10,

    // ส่วนลด/ส่วนเพิ่มราคาตาม tag ของผู้เล่น — คูณเข้ากับผลรวมค่าใช้จ่าย/วิ
    // ด้านบนทุกครั้งก่อนหักเงิน (ดู economyUtils.js: getTagPriceMultiplier)
    //
    // เพิ่ม tag ใหม่ = เพิ่ม 1 รายการที่นี่เท่านั้น ไม่ต้องแก้ autoCollect.js
    //   - multiplier < 1 = ลดราคา  เช่น 0.75 = ลด 25%
    //   - multiplier > 1 = เพิ่มราคา เช่น 1.25 = เพิ่ม 25%
    // ถ้าผู้เล่นมีหลาย tag ที่ตรงกันพร้อมกัน ตัวคูณจะถูกคูณต่อกันหมด (เช่น
    // มีทั้ง tag ลด 25% และ tag เพิ่ม 25% พร้อมกัน = 0.75 * 1.25 = 0.9375)
    //
    // ตัวอย่างด้านล่างคือ tag ตัวอย่าง ("vip" ลด 25%, "autocollect_surcharge"
    // เพิ่ม 25%) — แก้ชื่อ tag /ตัวเลข หรือลบ/เพิ่มรายการได้ตามต้องการ
    TAG_PRICE_MODIFIERS: [
      { tag: "vip", multiplier: 0.75 },
      { tag: "autocollect_surcharge", multiplier: 1.25 }
    ]
  }
};
