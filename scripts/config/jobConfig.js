// =========================
// jobConfig.js
// ค่าตั้งค่าระบบอาชีพ (Job Framework) ทั้งหมด — แก้ตัวเลขที่นี่จุดเดียว
// ไม่ต้องแก้ systems/jobSystem.js
// =========================

export const JOB_CONFIG = {
  /** เปลี่ยนอาชีพ (ครั้งแรกที่เลือกอาชีพเสมอฟรี ไม่มีคูลดาวน์ — เงื่อนไข
   *  ด้านล่างมีผลเฉพาะตอน "เปลี่ยน" จากอาชีพเดิมไปอาชีพอื่นเท่านั้น) */
  CHANGE: {
    COOLDOWN_SECONDS: 2 * 60 * 60, // 24 ชั่วโมง
    FEE: 50000 // เงินที่ต้องเสียเพื่อเปลี่ยนอาชีพ (หักหลังผ่านคูลดาวน์แล้วเท่านั้น)
  },

  /** ระบบเลเวล/exp ต่ออาชีพ (เก็บแยกกันต่ออาชีพต่อผู้เล่น — สลับอาชีพไปมา
   *  ไม่เสีย progress เดิม) */
  LEVEL: {
    MAX_LEVEL: 50,

    // exp ที่ต้องใช้เพื่อขึ้นเลเวลถัดไป = BASE_EXP * (GROWTH ^ (level - 1))
    // ปัดเป็นจำนวนเต็มเสมอ (เลเวล 1 -> 2 ใช้ BASE_EXP พอดี ยิ่งเลเวลสูงยิ่ง
    // ต้องการ exp มากขึ้นแบบทวีคูณ)
    BASE_EXP: 100,
    GROWTH: 1.15,

    // โบนัสเงินต่อ 1 เลเวล (คูณเข้ากับเงินรางวัลพื้นฐานของ action นั้น ๆ)
    // เลเวล 1 = ไม่มีโบนัส, เลเวล 2 = +2%, เลเวล 10 = +18% เป็นต้น
    // สูตร: multiplier = 1 + (level - 1) * MONEY_BONUS_PER_LEVEL
    MONEY_BONUS_PER_LEVEL: 0.02,

    // ตัวคูณ exp รวมทั้งระบบ ปรับได้ตรงนี้จุดเดียว มีผลกับทุกอาชีพ/action
    // พร้อมกัน (คูณเข้ากับ exp ฐานของแต่ละ reward ใน data/jobs.js ก่อน VIP
    // จะคูณซ้ำอีกที — ดู EXP_RATE ใน grantReward() ของ jobSystem.js)
    //   1.0  = ปกติ (ไม่ปรับ)
    //   1.25 = +25% (เช่นช่วงอีเว้นท์ exp คูณ)
    //   0.75 = -25% (เช่นลด exp รวมทั้งระบบ)
    EXP_RATE: 1.0
  },

  /** ระบบ VIP — ผู้เล่นที่มี tag ตามที่กำหนด (ตั้งเองผ่านคำสั่งในเกม เช่น
   *  /tag <player> add vip) จะได้ตัวคูณเงิน/exp รางวัลอาชีพเพิ่มพิเศษ ใช้
   *  getTagPriceMultiplier() ตัวเดียวกับที่ AUTO_COLLECT ใน
   *  config/economyConfig.js ใช้ (core/economyUtils.js) ไม่ต้องเขียนตัว
   *  เทียบ tag ซ้ำเอง
   *
   *  moneyMultiplier / expMultiplier: 1.25 = +25%, 0.75 = -25% ฯลฯ —
   *  ตัวคูณนี้คูณต่อจาก EXP_RATE / MONEY_BONUS_PER_LEVEL ด้านบนอีกที
   *  (ไม่ใช่แทนที่) เพิ่ม tag ใหม่ = เพิ่ม 1 รายการที่นี่เท่านั้น ไม่ต้อง
   *  แก้ jobSystem.js — ถ้าผู้เล่นมีหลาย tag ที่ตรงกันพร้อมกัน ตัวคูณจะถูก
   *  คูณต่อกันหมด (เหมือนแพทเทิร์นเดียวกับ AUTO_COLLECT.TAG_PRICE_MODIFIERS) */
  VIP: {
    TAG_MODIFIERS: [
      { tag: "vip", moneyMultiplier: 1.50, expMultiplier: 2.25 }
    ]
  },

  /** เพิร์คพิเศษต่ออาชีพ (เอฟเฟกต์ passive ขณะกำลังถืออาชีพนั้นอยู่) —
   *  แต่ละอาชีพระบุแค่ "ชนิดเอฟเฟกต์" ของตัวเองไว้ที่ data/jobs.js
   *  (field: perkEffect) ส่วนเลเวลที่ปลดล็อกแต่ละระดับ (I/II/III) ใช้
   *  ตารางเดียวกันร่วมกันทุกอาชีพตรงนี้ — แก้ที่นี่จุดเดียวมีผลกับทุกอาชีพ
   *  พร้อมกัน ไม่ต้องไปแก้ทีละอาชีพใน data/jobs.js
   *
   *  ระบบรีเฟรชเอฟเฟกต์ให้ผู้เล่นที่ออนไลน์ทุก REFRESH_INTERVAL_TICKS วิ
   *  (ดู jobSystem.js) — EFFECT_DURATION_TICKS ต้องนานกว่า
   *  REFRESH_INTERVAL_TICKS เสมอ (กันเอฟเฟกต์หมดอายุ/กระพริบในช่วงรอยต่อ
   *  ระหว่างรอบรีเฟรช) พอเปลี่ยน/เลิกอาชีพ เอฟเฟกต์จะค่อย ๆ หมดไปเองภายใน
   *  ไม่กี่วินาที ไม่ต้องคอยเคลียร์เอฟเฟกต์ตรง ๆ ตอนเปลี่ยนอาชีพ */
  PERK: {
    ENABLED: true,
    REFRESH_INTERVAL_TICKS: 100, // 5 วินาที
    EFFECT_DURATION_TICKS: 140, // 7 วินาที (> REFRESH_INTERVAL_TICKS)

    // amplifier ของเอฟเฟกต์ Minecraft นับจาก 0 (0 = ระดับ I, 1 = ระดับ II
    // ฯลฯ) — ต้องเรียงเลเวลจากน้อยไปมากเสมอ
    TIERS: [
      { level: 15, amplifier: 0 },
      { level: 30, amplifier: 1 },
      { level: 50, amplifier: 2 }
    ]
  }
};
