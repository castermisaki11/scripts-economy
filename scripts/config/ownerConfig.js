// =========================
// config/ownerConfig.js
// รายชื่อผู้เล่นที่ได้สิทธิ์ owner ของแอดออน — ย้ายมาจาก hardcode ใน
// systems/ownerSetup.js เดิม เพื่อให้ค่าตั้งค่าทั้งหมดอยู่ที่ config/
// ตามกฎ CONTRIBUTING.md
// =========================

/**
 * รายชื่อผู้เล่น (player.name ตัวพิมพ์เล็ก-ใหญ่ตรงตามที่แสดงในเกม) ที่จะได้
 * แท็ก admin อัตโนมัติทันทีที่เข้าเกมครั้งแรก (ดู systems/ownerSetup.js)
 * เพิ่ม/ลบชื่อที่ array นี้จุดเดียว
 */
export const OWNER_PLAYER_NAMES = ["castermisaki897"];
