// =========================
// shopConfig.js
// ค่าตั้งค่า "ร้านค้า/ตลาด" ทั้งหมดที่เป็นตัวเลข/ธุรกิจ (ไม่ใช่ข้อมูลไอเทม
// หรือข้อมูลเอฟเฟกต์ — ดูที่ data/items.js / data/effects.js) รวมไว้จุด
// เดียว — ย้ายมาจากค่าคงที่ที่กระจายอยู่คนละไฟล์เดิม:
//   - BUY_AMOUNT_PRESETS: เดิม hardcode อยู่ใน systems/shopSystem.js
//     (openBuyItemMenu)
//   - EFFECT_SHOP.*: เดิม hardcode อยู่ใน systems/shopEffect.js
//     (openPresetMenu — BASE, times, amps ของแต่ละสูตรคำนวณราคา)
//   - MARKET.*: เดิม hardcode อยู่ใน systems/playerMarket.js
//     (TAX_RATE / MAX_MARKET_LISTINGS / MAX_LISTING_PRICE /
//     LISTING_EXPIRE_DAYS)
// =========================

export const SHOP_CONFIG = {
  /** จำนวนพรีเซ็ตให้เลือกตอนซื้อไอเทม (นอกเหนือจากกรอกจำนวนเอง) */
  BUY_AMOUNT_PRESETS: [1, 3, 32, 64],

  /** ร้านค้าเอฟเฟกต์ (potion effect) — พารามิเตอร์คำนวณราคาตามระยะเวลา/ระดับ */
  EFFECT_SHOP: {
    // หน่วยฐานที่ cost ต่อระดับของแต่ละเอฟเฟกต์อ้างอิงถึง (วินาที)
    BASE_UNIT_SECONDS: 60,
    // ตัวเลือกระยะเวลา (วินาที) สำหรับเอฟเฟกต์ที่ไม่มีระดับ (singleLevel: true)
    SINGLE_LEVEL_DURATIONS: [600, 1200, 1800, 3600],
    // ตัวเลือกระดับ/ระยะเวลา (วินาที) สำหรับเอฟเฟกต์ที่มีหลายระดับ
    LEVELED_AMPS: [1, 2, 3, 4, 5],
    LEVELED_DURATIONS: [30, 60, 600, 1200]
  },

  /** ร้านค้า NPC (ซื้อของ/เอฟเฟกต์จากร้าน) — เงินที่ผู้เล่นจ่ายจะเข้า
   *  ธนาคารกลางแค่ "ส่วนภาษี" (TAX_RATE) เท่านั้น ไม่ใช่เต็มจำนวนที่จ่าย
   *  (แก้บั๊ก: เดิมเข้าธนาคารกลางเต็ม 100% ของเงินที่ใช้ ทำให้ "ภาษีสะสม"
   *  ในหน้า Admin พุ่งขึ้นเท่ากับยอดที่ซื้อเป๊ะ ๆ แทนที่จะเป็น 10% เหมือน
   *  ภาษีตลาด/ภาษีโอนเงิน) — ใช้ค่าเดียวกับ MARKET.TAX_RATE ด้านล่างเพื่อให้
   *  ภาษีทุกระบบในเกมเท่ากันหมด ส่วนที่เหลือ (90%) หายออกจากระบบเศรษฐกิจ
   *  ตามปกติเหมือนร้านค้า NPC ทั่วไป */
  NPC_SHOP: {
    TAX_RATE: 0.1
  },

  /** ตลาดผู้เล่น (Player Market) */
  MARKET: {
    TAX_RATE: 0.1,
    MAX_LISTINGS: 200,
    MAX_LISTING_PRICE: 1_000_000_000,
    LISTING_EXPIRE_DAYS: 1,
    // ความยาวสูงสุดของ "รายละเอียดสินค้า" ที่ผู้ขายพิมพ์ไว้ตอนลงขาย (ตัวอักษร)
    // ยาวเกินจะถูกตัดทิ้งส่วนที่เหลืออัตโนมัติ ไม่ปฏิเสธการลงขาย
    MAX_DESCRIPTION_LENGTH: 100
  }
};
